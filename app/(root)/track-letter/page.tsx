import SectionHeader from "@/components/SectionHeader";
import TopHeader from "@/components/TopHeader";
import React from "react";

const TrackLetter = () => {
  return (
    <section>
      <TopHeader />
      <SectionHeader title="Track Letter" />
    </section>
  );
};

export default TrackLetter;
